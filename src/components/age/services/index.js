const createAge = require('./createAge')
const deleteAge = require('./deleteAge')
const getAges = require('./getAges')
const updateAge = require('./updateAge')

module.exports = {
  createAge,
  deleteAge,
  getAges,
  updateAge
}

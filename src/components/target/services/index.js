const getTartes = require('./getTartes')
const createTarget = require('./createTarget')
const updateTarget = require('./updateTarget')
const deleteTarget = require('./deleteTarget')

module.exports = {
  getTartes,
  createTarget,
  updateTarget,
  deleteTarget
}
